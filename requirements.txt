# Layer 1 — Reinforcement Learning
gymnasium>=0.29.1
numpy>=1.24.0
torch>=2.0.0
matplotlib>=3.7.0

# Layer 2 — Real-time server (used in later steps)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-socketio>=5.11.0
